# -----------------------------------------
# Environment Variables for SBOM Aggregator
# -----------------------------------------

# Node environment: development or production
NODE_ENV=development

# MongoDB connection (secrets loaded via Docker secrets)
MONGO_INITDB_ROOT_USERNAME_FILE=/run/secrets/mongo_root_user
MONGO_INITDB_ROOT_PASSWORD_FILE=/run/secrets/mongo_root_pass
MONGO_PASSWORD=strong_db_password

# MQTT Broker (EMQX) URL and credentials
MQTT_USERNAME=mqttUser
MQTT_PASSWORD=mqttPass

# JWT secret for API authentication
JWT_SECRET=pls_replace_me_with_a_strong_jwt_secret

# Optional: path to CA bundle for MQTT TLS
# MQTT_CA_FILE=/app/config/ca.crt
